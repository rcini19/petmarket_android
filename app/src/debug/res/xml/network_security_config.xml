<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may call a local backend over HTTP from an emulator or a physical phone. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
